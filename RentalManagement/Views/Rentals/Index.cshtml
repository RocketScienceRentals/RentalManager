@model IEnumerable<RentalManagement.Models.Rental>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NegotiatedOn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClientID.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssetID.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NegotiatedOn)
        </td>
        <th>
            @Html.DisplayFor(modelItem => item.ClientID.Name)
        </th>
        <th>
            @Html.DisplayFor(modelItem => item.AssetID.Name)
        </th>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
